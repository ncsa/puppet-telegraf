---
lookup_options:
  'telegraf::inputs':
    merge:
      strategy: deep
      merge_hash_arrays: true
      knockout_prefix: "--"
  'telegraf::outputs':
    merge:
      strategy: deep
      merge_hash_arrays: true
      knockout_prefix: "--"
